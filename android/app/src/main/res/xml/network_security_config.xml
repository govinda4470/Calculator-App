<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production traffic must use TLS. Trust only the Android system store;
         user-installed CAs are intentionally not trusted by release builds. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
